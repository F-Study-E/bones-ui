---
"@bones/react": minor
"@bones/hooks": minor
---

`Switch` 컴포넌트와 `useControllableState` 훅 첫 구현. `Switch` + `Switch.Thumb` 합성, `checked` / `defaultChecked` / `onCheckedChange` 제어 인터페이스, hidden checkbox를 통한 form 통합, `asChild`와 `data-state` 이중 부착 지원.
